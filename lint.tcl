# look for bare $variables rather than ${var}


# look for redefinition command

# list all variables, sorted
# mention their length

# list all procedure names, sorted
# bracket depth checker
# count lines, whitespace, comments
# watch for no indentations -> fix indentation based on bracket depth
# count inline comments
# find comments containing likely code: { }
